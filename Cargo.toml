[package]
name = "relay"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "signal"] }
axum = "0.8"
# No gzip/brotli/zstd feature, deliberately: with one, reqwest would silently
# decompress upstream bodies while the proxy still forwards `content-encoding`,
# mislabelling every compressed response. Held by the gzip fidelity test in
# tests/proxy.rs.
reqwest = { version = "0.12", default-features = false, features = ["stream", "rustls-tls"] }
futures-core = "0.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.9"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
sha2 = "0.11.0"
base64 = "0.23.1"
